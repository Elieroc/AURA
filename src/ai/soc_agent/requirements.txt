psycopg[binary]==3.3.4
requests==2.32.5
PyYAML==6.0.2
dfir-iris-client>=2.0.4
# Rendu du rapport mensuel (soc_agent.monthly_report). IRIS a son propre
# moteur Jinja côté serveur pour les rapports de case ; celui-ci sert
# uniquement le template hors-case rapport-mensuel-fr.md.
Jinja2==3.1.6
# Client S3 de l'archivage à froid (soc_agent.archive). Utilisé contre
# l'endpoint S3-compatible de Backblaze B2, pas contre AWS.
boto3==1.43.71
