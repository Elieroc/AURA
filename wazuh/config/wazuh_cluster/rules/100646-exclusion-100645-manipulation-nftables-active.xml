<!-- SOC-AI - regle 100646 (niveau 0). Domaine : threat_hunting,linux,mitre_gaps,
     Convention et piege d ordre de chargement : voir rules/README.md -->
<group name="threat_hunting,linux,mitre_gaps,">

  <!-- Exclusion 100645 : les active responses du SOC pilotent nftables par
       conception (isolation et lever d'isolation). Leur parent est le script AR,
       pas un shell d'attaquant. -->

  <rule id="100646" level="0">
    <if_sid>100645</if_sid>
    <regex type="pcre2">host-(?:un)?isolate|66697265776</regex>
    <description>Exclusion 100645 : manipulation nftables par une active response SOC-AI</description>
  </rule>

</group>
