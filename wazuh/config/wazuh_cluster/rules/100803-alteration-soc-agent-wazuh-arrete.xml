<!-- SOC-AI - regle 100803 (niveau 12). Domaine : soc_selfcheck,wazuh,
     Convention et piege d ordre de chargement : voir rules/README.md -->
<group name="soc_selfcheck,wazuh,">

  <!-- Promotion de la regle native 506 (niveau 3). Arreter l'agent est LA
       premiere action d'un attaquant qui a obtenu root, et c'est une ACTION
       deliberee (contrairement a une deconnexion reseau) : niveau 12 pour que le
       pipeline la traite. Effet de bord assume : une maintenance sur l'agent
       genere une alerte HIGH. C'est le prix a payer pour ne pas rater l'inverse,
       et la whitelist automatique absorbera la recurrence si elle apparait. -->

  <rule id="100803" level="12">
    <if_sid>506</if_sid>
    <description>Alteration du SOC : agent Wazuh arrete</description>
    <mitre>
      <id>T1562.001</id>
    </mitre>
  </rule>

</group>
